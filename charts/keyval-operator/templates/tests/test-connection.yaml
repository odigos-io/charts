apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "keyval-operator.fullname" . }}-test-connection"
  labels:
    {{- include "keyval-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "keyval-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
