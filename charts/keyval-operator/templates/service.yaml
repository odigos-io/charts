apiVersion: v1
kind: Service
metadata:
  name: {{ include "keyval-operator.fullname" . }}
  labels:
    {{- include "keyval-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "keyval-operator.selectorLabels" . | nindent 4 }}
